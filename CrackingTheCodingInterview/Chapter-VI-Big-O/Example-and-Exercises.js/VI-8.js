//You are looking for a specific values in a binary tree, but the tree is not a binary search tree. What is the time complexity of this? 

//Answer: O(n), Without any ordering property on the nodes, we might have to search through all the nodes (with a simple loop).